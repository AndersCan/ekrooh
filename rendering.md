# Nanostores + lit-html rendering

The full, authoritative rendering contract now lives on the published docs site:

**Web Rendering** — `apps/docs/src/content/docs/consumers/rendering.mdx`

That page is the single source of truth (core rule, the `useStore` footgun,
store patterns, lists, routing, and memory teardown). This file is kept as a
signpost so existing references resolve; the site doc is what you should edit.
