pluginManagement {
  repositories {
    google()
    mavenCentral()
    gradlePluginPortal()
  }
}

dependencyResolutionManagement {
  repositories {
    google()
    mavenCentral()
  }
}

include ":bare-host"
project(":bare-host").projectDir = file("android")

include ":examples:android-app"
project(":examples:android-app").projectDir = file("examples/android-app")
